{
  "name": "abundance-farm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash bin/deploy.sh",
    "deploy:bare": "wrangler deploy",
    "deploy:dry": "bash bin/deploy.sh --dry-run",
    "deploy:rotate-secret": "bash bin/deploy.sh --rotate-secret",
    "tail": "wrangler tail",
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
